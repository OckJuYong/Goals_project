<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mocom_project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mocom_project">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SignupPage"
            android:exported="false"/>

        <activity
            android:name=".MainPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AreaChoiceActivity"
            android:exported="true"/>

        <activity
            android:name=".MainGuidePage"
            android:exported="false"/>

        <activity
            android:name=".NextActivity"
            android:exported="false"/>

        <activity
            android:name=".InputInfoActivity"
            android:label="Input Info Activity"/>

        <activity
            android:name=".DetailActivity"
            android:label="Detail Activity"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA9R0NUEETArKdIaSI9QaVclSMz4QJTrS4"/>
    </application>
</manifest>
